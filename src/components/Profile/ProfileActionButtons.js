// // ProfileActionButtons.js
// import React from 'react';
// import { EditButton, SaveButton, SignOutButton } from '../../../styles/ProfileStyles';

// const ProfileActionButtons = ({ isEditing, handleSave, handleEdit, handleSignOut }) => {
//   return !isEditing ? (
//     <>
//       <EditButton onClick={handleEdit}>Edit Profile</EditButton>
//       <SignOutButton onClick={handleSignOut}>Sign Out</SignOutButton>
//     </>
//   ) : (
//     <SaveButton onClick={handleSave}>Save</SaveButton>
//   );
// };

// export default ProfileActionButtons;
